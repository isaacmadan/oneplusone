<div class="container">
	<%= form_for @team do |f| %>
		<div class="form-group">
			<%= f.label :name, "Name" %>
			<%= f.text_field :name, :class => "form-control" %>
		</div>

		<div class="form-group">
			<%= f.submit "Submit", :class => "btn btn-default" %>
		</div>
	<% end %>
</div>